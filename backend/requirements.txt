# Backend dependencies for the Neurosymbolic road defect assessment API.
# Verified against Python 3.14.6 on Windows (the interpreter this project uses).
#
#   python -m venv deps
#   deps\Scripts\activate
#   pip install -r backend/requirements.txt
#
# Model weights are NOT pip-installable and are not in the repo. See Setup & Run:
#   backend/ultrabestroad.pt          road segmentation (YOLOv8)
#   backend/best.pt                   crack detection (YOLOv8-seg)
#   backend/sam_files/sam_vit_b_01ec64.pth
#     https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth

# --- Web server ---
fastapi==0.138.2
uvicorn==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2

# --- Neural component ---
torch==2.13.0
torchvision==0.28.0
ultralytics==8.4.108
segment-anything==1.0

# --- Spatial component (IPM, skeletonization) ---
opencv-python==5.0.0.93
numpy==2.5.0

# --- Symbolic component: Fuzzy Logic engine (Phase 2, decision D1) ---
scikit-fuzzy==0.5.0
scipy==1.16.2
networkx==3.6.1

# --- Local LLM bulletin via Ollama (decision D2) ---
# No Python SDK needed; the model is reached over HTTP at OLLAMA_HOST.
requests==2.34.2
